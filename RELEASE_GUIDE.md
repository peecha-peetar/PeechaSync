# راهنمای انتشار نسخه‌ی جدید PeechaSync

این راهنما رو بعد از بررسی کامل سورس پروژه نوشتم — همه‌ی ابزارهای لازم از قبل
تو پروژه هستن، فقط باید بدونید کدوم رو کِی اجرا کنید.

## ⚠️ یه نکته‌ی مهم قبل از هرچی
فایل `PeechaSync.spec` (تنظیمات PyInstaller) که تو ریشه‌ی پروژه هست، **هیچ‌جای
این خط‌لوله‌ی واقعی استفاده نمی‌شه**. روش واقعی توزیع پروژه، PyInstaller
نیست — یه پایتون قابل‌حمل (Embeddable) + کد خام + پکیج‌های آفلاین (wheel) +
کامپایل به bytecode (`.pyc`) است. یعنی نیازی نیست نگران به‌روز نگه‌داشتن اون
فایل `.spec` باشید؛ کافیه وابستگی‌های جدید رو به
`release\requirements-client.txt` اضافه کنید (که همیشه انجامش می‌دم).

---

## ✅ کار روزمره (بعد از هر تغییر کد)

### مرحله‌ی صفر — فقط یک‌بار در کل عمر پروژه
```
tools\Setup_WP_LicenseDeploy.bat
```
یوزر/رمز FTP سایت (از پنل هاست) رو داخل فایلی که باز می‌شه پر و Save کنید.

### مرحله‌ی ۱ — نسخه رو بالا ببرید
`sync_app\core\app_version.py` → مقدار `_BUILTIN_VERSION` رو عوض کنید.

### مرحله‌ی ۲ — یک دستور، همه‌کار
```
tools\Run_Release_All.bat
```
این اسکریپت به‌ترتیب:
1. یه ZIP کامل از برنامه (کد + پایتون قابل‌حمل + پکیج‌های آفلاین) می‌سازه
2. دو بار مکث می‌کنه و ازتون می‌خواد VPN رو روشن/خاموش کنید:
   - **VPN روشن** → برای `git push` + ساخت Release روی GitHub
   - **VPN خاموش** → برای آپلود FTP مستقیم به `peecha.ir` (هاست ایران)
3. ZIP نهایی رو هم به‌صورت `PeechaSync-X.Y.Z.zip` هم `latest.zip` روی
   `wp-content/uploads/peecha-sync-updates/` می‌ذاره

بعدش برید تو وردپرس: **لایسنس‌های پیچا → آخرین نسخه‌ی برنامه** رو همون شماره
نسخه بزنید (یا از اسم فایل خودکار خونده می‌شه) و changelog رو پر کنید.

### گزینه‌های کمکی (در صورت نیاز)
```
tools\Run_Release_All.bat            (یا مستقیم: powershell tools\Publish-Release.ps1)
  -SkipMirror        فقط GitHub، بدون آپلود روی سایت
  -SkipGit           فقط آپلود روی سایت، بدون GitHub
  -NoPrompt          بدون مکث VPN (اگه هردو بدون فیلترشکن در دسترسن)
  -DeployPlugin      افزونه‌ی وردپرس رو هم همزمان آپدیت کن
```

---

## اگه فقط بخواید یه فایل نصبیِ محلی بسازید (بدون انتشار)
```
tools\Run_Build_SetupPackage.bat
```
خروجی: یه پوشه/ZIP نصبی که می‌تونید مستقیم به مشتری بدید (بدون نیاز به
GitHub/سایت) — همون چیزی که `release\installer\templates\Install PeechaSync.bat`
اجراش می‌کنه.

## اگه فقط بخواید افزونه‌ی وردپرس رو (بدون کلاینت) آپدیت کنید
```
tools\Run_Deploy_WP_LicensePlugin.bat
```

## اگه چیزی خراب شد / گیر کرد
```
tools\Check-PeechaSync.bat          بررسی سلامت نصب فعلی
tools\Repair-FullInstall.bat        تعمیر کامل نصب محلی
tools\Test-WP-LicenseFtp.bat        فقط تست اتصال FTP (بدون انتشار واقعی)
```

---

## خلاصه‌ی خیلی کوتاه
```
1) sync_app\core\app_version.py رو عوض کن
2) tools\Run_Release_All.bat رو بزن
3) موقع مکث‌ها VPN رو روشن/خاموش کن
4) تو وردپرس، «آخرین نسخه‌ی برنامه» رو آپدیت کن
```
